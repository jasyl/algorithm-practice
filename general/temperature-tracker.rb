class TempTracker

  # records a new temperature
  def insert(temperature)
  end

  # returns the highest temp we've seen so far
  def get_max()
  end

  # returns the lowest temp we've seen so far
  def get_min()
  end

  # returns the mean (average) of all temps we've seen so far
  # should return a float
  def get_mean()
  end

  # returns a mode (values that appear the most) of all temps we've seen so far
  def get_mode()
  end
end